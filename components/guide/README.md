# Components Guide

- `components/layout`: page shell composition only.
- `components/elements`: reusable visual building blocks.
- `components/ui`: low-level UI primitives.
- `components/logic`: interaction and selection rules.
- `components/estate`: app-specific components for this repository.

Variants use suffixes like `v1` and `v2` so a component can evolve without breaking imports.

